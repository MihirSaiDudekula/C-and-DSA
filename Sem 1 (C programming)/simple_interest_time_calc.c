#include <stdio.h>
int main()
{
	float p=20000.0;
	float r=5.6;
	float t;
	// intf("\n enter principle rate and time for SI ");
	// scanf("%f%f%f",&p,&r,&t);
	float s=10000.0;
	t=((100*s)/(p*r));
	printf("\n The time required is %f ",t);
	return 0;

}
